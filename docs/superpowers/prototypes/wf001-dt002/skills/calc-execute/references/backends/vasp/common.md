# VASP common execution knowledge

Load this file for every VASP task. The task declaration and approved Spec are
the physical parameter authority; templates are only approved input baselines.

Before submission, require one exact prepared Run snapshot, a readable cluster
profile, matching PBS/submission scripts, and transient `calc-review` approval
for that snapshot. A review does not prove execution or create outputs.

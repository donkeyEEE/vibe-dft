---
name: cangjie-skill
description: Use when the user explicitly invokes Cangjie while its resource-distillation workflow is frozen pending redesign.
---

# Cangjie — frozen

Cangjie's resource-writing and governance workflow is suspended while its new
ownership model is designed.

When invoked, report that the skill is retained for future redesign but cannot
currently distill, write, admit, promote, or modify repository resources. Ask
the user to use an ordinary summary or research workflow if that satisfies
their immediate goal. End without creating or changing files.

The material under `references/legacy/` records the retired workflow for future
design work. Load it only when the user explicitly asks to redesign Cangjie;
it is historical input, not executable instructions.

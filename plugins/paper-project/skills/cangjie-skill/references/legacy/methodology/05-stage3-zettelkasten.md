# 阶段 3 — 共享索引纳入

通用 ATOM CARD 平铺写入 `plugins/paper-project/knowledge/cards/atoms/`；物理卡按概念、现象、理论与模型写入独立子库。

1. 使用相应索引已登记的 `phys-`、`comp-`、`write-`、`journal-` 或 `meta-` 前缀。
2. 先检查是否已有相同原子主张；已有时更新该卡而不是创建近似重复卡。
3. 将每个 tag 加入相应索引；若需要新 tag 或 type，先登记其含义，再写卡。
4. 卡片发生冲突时保留各自范围或请用户决定，不将冲突内容伪装为同一结论。

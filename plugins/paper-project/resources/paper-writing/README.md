# Shared paper-writing resources

These evidence-bearing references are shared only where current skills have
demonstrated consumption. Consumers link the exact files they load; this
directory is not a runtime discovery index.

| Resource group | Active consumers |
|---|---|
| `write-terminology-ledger.md` | `prl-polishing`, `paper2ppt` |
| `write-prl-*.md` | `prl-polishing`, `prl-figure` |

Missing resources are reported as optional-resource failures. Consumers may
continue their primary workflow without substituting another resource source.

interface:
  display_name: "Show COT"
  short_description: "只读展示计算项目的 RQ、Spec、Task 与 Run 全部历史"
  default_prompt: "$show-cot 展示此计算项目的完整 Calculation Ownership Tree。"

policy:
  allow_implicit_invocation: false
